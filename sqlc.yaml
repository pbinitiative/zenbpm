version: "2"
sql:
    - engine: "sqlite"
      queries:
          - "internal/sql/queries"
      schema: "internal/sql/migrations"
      gen:
          go:
              package: "sql"
              out: "internal/sql"
              emit_json_tags: true
              emit_empty_slices: true
              emit_interface: true
